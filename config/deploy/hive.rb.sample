# config how to access the server

# server 'example.com', user: 'deploy', roles: %w(hive), my_property: :my_value
# server 'db.example.com', user: 'deploy', roles: %w{hive}
# role :hive, %w{deploy@example.com}, my_property: :my_value
# role :hive, %w{user1@primary.com user2@additional.com}, other_property: :other_value
# role :hive,  %w{deploy@example.com}
